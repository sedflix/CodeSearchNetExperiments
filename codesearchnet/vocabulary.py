# from .code_parser import get_parser, parse_program, get_tokens_from_graph
# import swifter
import pandas as pd
from tree_sitter import Parser


def create_code_corpus(df: pd.DataFrame, so_path):
    df.swifter.apply()


def _create_query_corpus(x):
    pass
